[pytest]
testpaths = tests
asyncio_mode = auto
asyncio_default_fixture_loop_scope = function
timeout = 30
markers =
    live: touches real network endpoints (deselect with -m "not live")
    llm: spends real LLM tokens, one call per provider (deselect with -m "not llm")

services:
  microagent:
    build: .
    container_name: microagent
    restart: unless-stopped
    volumes:
      - ./soul:/soul:ro
      - agent-data:/data
    environment:
      - SOUL_DIR=/soul
      - DATA_DIR=/data

volumes:
  agent-data:
